# Stage 5 — CI and parser-core stabilization

- Configure pytest to import repository-root modules.
- Make tests an explicit package.
- Add import smoke test.
- Add stable normalizer contract test.
- Keep marketplace access failures isolated in provider results.
